# Resource Group Configuration
RG=your-project-rg
LOCATION=westus2

# Alert Configuration
ALERT_EMAIL=your-email@example.com

# Heartbeat Agent Configuration
HEARTBEAT_URL=https://your-func.azurewebsites.net/api/ping
HEARTBEAT_DEVICE=your-device-name
HEARTBEAT_INTERVAL=60

# Note: Azure Action Groups must use location='global' (handled in main.bicep)
# The LOCATION variable above is only for regional resources (storage, function app, etc.)
